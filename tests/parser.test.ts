import {describe,expect,it} from "vitest";import {DeterministicCriteriaParser} from "../src/lib/ai/parser";import {searchCriteriaSchema} from "../src/schemas/criteria";describe("AI parser boundary",()=>{it("returns schema-valid shared criteria",async()=>expect(searchCriteriaSchema.safeParse(await new DeterministicCriteriaParser().parse("Middleton £1m schools")).success).toBe(true));it("parses a written million-pound budget",async()=>expect((await new DeterministicCriteriaParser().parse("We have a £1 million budget")).property?.maximumBudget).toBe(1_000_000));it("handles missing optional inputs",async()=>expect((await new DeterministicCriteriaParser().parse("I need a place")).property?.maximumBudget).toBeDefined())});
