import { searchCriteriaSchema } from "@/src/schemas/criteria";
import type { SearchCriteria } from "@/src/types/domain";
export interface CriteriaParser { parse(text:string):Promise<SearchCriteria> }
export class DeterministicCriteriaParser implements CriteriaParser { async parse(text:string){const lower=text.toLowerCase();const budgetMatch=lower.match(/(?:£|\b)([\d.]+)\s*(m|million|k)?/);let budget=1000000;if(budgetMatch){budget=Number(budgetMatch[1])*(budgetMatch[2]?.startsWith("m")?1000000:budgetMatch[2]==="k"?1000:1)}const minuteMatch=lower.match(/(\d+)\s*min/);const criteria:SearchCriteria={mode:lower.includes("job")||lower.includes("work area")?"work":"live",destinations:[{id:"middleton-work",label:"Middleton",purpose:"My work",latitude:53.55,longitude:-2.2,journeysPerWeek:lower.includes("five")?5:5,preferredMinutes:Number(minuteMatch?.[1]??35),maximumMinutes:50,transportMode:"drive",weight:100,hardMaximum:false}],property:{maximumBudget:budget,hardBudget:false,minimumBedrooms:3,tenure:"buy"},lifestyle:[{key:"schools",weight:lower.includes("school")?90:50},{key:"greenSpace",weight:lower.includes("rural")||lower.includes("countryside")?95:50},{key:"rurality",weight:lower.includes("rural")?90:45},{key:"amenities",weight:65},{key:"restaurants",weight:lower.includes("restaurant")?65:40},{key:"lowCrime",weight:85},{key:"railAccess",weight:30},{key:"broadband",weight:55}]};return searchCriteriaSchema.parse(criteria) as SearchCriteria;} }
export interface ResultExplainer { explain(criteria:SearchCriteria,results:import("@/src/types/domain").LocationScore[]):Promise<string> }
export class DeterministicResultExplainer implements ResultExplainer { async explain(_:SearchCriteria,r:import("@/src/types/domain").LocationScore[]){const [a,b]=r;return `${a.town} ranks first because ${a.positives.slice(0,2).join(" and ")}. ${b?.town??"The next option"} offers a different balance but gives up ${a.town}'s strongest advantages.`;} }
